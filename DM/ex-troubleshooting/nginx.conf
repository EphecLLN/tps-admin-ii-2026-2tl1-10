events {}

http {
    upstream backend {
        server web1:80 max_fails=0;
        server web2:80 max_fails=0;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
            
            proxy_next_upstream off; 
        }
    }
}
